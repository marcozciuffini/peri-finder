export type AppTheme = {
  dark: boolean;
  colors: ThemeColors;
};

export type ThemeColors = {
  background: string;
  surface: string;
  border: string;
  text: string;
  icon: string;
  tint: string;
  error: string;
  accent: string;
  subtle: string;
};
